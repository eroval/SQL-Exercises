SELECT ROUND(5.73) -- rounds to 6
SELECT ROUND(5.73, 1) -- rounds to 1 after decimal
SELECT TRUNCATE(5.7345, 2) -- keeps the 2 digits after decimal and removes the others
SELECT CEILING(5.7) -- smallest integer that is GREATER than the number
SELECT FLOOR(5.7) -- smallest integer that is LESSER than the number
SELECT ABS(-4) -- absolute value
SELECT RAND() -- randomized values
